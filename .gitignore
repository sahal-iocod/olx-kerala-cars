# Secrets — never commit the real token/chat id
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Runtime state (regenerated by the app)
seen_ads.json
location_cache.json
browser_state.json
bot.log

# Debug artifacts written on each scrape
olx_debug.html
olx_api_debug.json
olx_location_debug.png

# Misc
olx-kerala-cars.zip
.pytest_cache/
.DS_Store
